generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model portal_calendarEvent {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_calendarEventParticipant {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_calendarEventShare {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_calendarFolder {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_company {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  address     String
  city        String
  country     String
  createdAt   DateTime @db.Date
  description String
  email       String
  industry    String
  /// Could not determine type: the field only had null or empty values in the sample set.
  metadata    Json?
  name        String
  phone       String
  postalCode  String
  size        String
  state       String
  status      String
  updatedAt   DateTime @db.Date
  website     String
}

model portal_companyContact {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_companyCustomField {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_companyCustomValue {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_contact {
  id               String   @id @default(auto()) @map("_id") @db.ObjectId
  /// Could not determine type: the field only had null or empty values in the sample set.
  address          Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  assignedTo       Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  city             Json?
  company          String
  /// Could not determine type: the field only had null or empty values in the sample set.
  companyId        Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  country          Json?
  createdAt        DateTime @db.Date
  /// Could not determine type: the field only had null or empty values in the sample set.
  currentRole      Json?
  email            String
  /// Could not determine type: the field only had null or empty values in the sample set.
  externalId       Json?
  firstName        String
  /// Could not determine type: the field only had null or empty values in the sample set.
  gender           Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  industry         Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  jobTitle         Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  joinedAt         Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  lastContactedAt  Json?
  lastName         String
  name             String
  /// Could not determine type: the field only had null or empty values in the sample set.
  nextFollowUpAt   Json?
  phone            String
  /// Could not determine type: the field only had null or empty values in the sample set.
  postalCode       Json?
  priority         String
  remark           String
  /// Could not determine type: the field only had null or empty values in the sample set.
  skills           Json?
  source           String
  /// Could not determine type: the field only had null or empty values in the sample set.
  state            Json?
  status           String
  /// Could not determine type: the field only had null or empty values in the sample set.
  stripeCustomerId Json?
  updatedAt        DateTime @db.Date
  /// Could not determine type: the field only had null or empty values in the sample set.
  workExperience   Json?
}

model portal_contactActivity {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  contactId     String
  createdAt     DateTime @db.Date
  description   String
  initiatorId   String
  initiatorType String
  metadata      String
  subType       String
  type          String
  updatedAt     DateTime @db.Date
  userId        String
}

model portal_contactCampaign {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_contactCustomField {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_contactCustomValue {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_contactDeal {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_marketingCampaign {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_paymentTrack {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_resourceContent {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_resourceContentSendTrack {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_resourceContentShare {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_resourceEmails {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_session {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_siteConfig {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_subscriptionCoupon {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_subscriptionPlan {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_team {
  id           String   @id @default(auto()) @map("_id") @db.ObjectId
  campaignCode String
  companyId    String
  createdAt    DateTime @db.Date
  createdBy    String
  description  String
  /// Could not determine type: the field only had null or empty values in the sample set.
  leaderId     Json?
  name         String
  /// Could not determine type: the field only had null or empty values in the sample set.
  referralId   Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  remarks      Json?
  /// Could not determine type: the field only had null or empty values in the sample set.
  subLeaderId  Json?
  updatedAt    DateTime @db.Date
}

model portal_teamActivity {
  id            String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime @db.Date
  description   String
  initiatorId   String
  initiatorType String
  metadata      String
  subType       String
  teamId        String
  type          String
  updatedAt     DateTime @db.Date
  userId        String
}

model portal_teamContact {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  /// Could not determine type: the field only had null or empty values in the sample set.
  assignedTo Json?
  contactId  String
  createdAt  DateTime @db.Date
  teamId     String
  updatedAt  DateTime @db.Date
}

model portal_teamCustomField {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_teamCustomValue {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_teamMeeting {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_user {
  id            String @id @default(auto()) @map("_id") @db.ObjectId
  email         String
  /// Could not determine type: the field only had null or empty values in the sample set.
  emailVerified Json?
  firstName     String
  /// Could not determine type: the field only had null or empty values in the sample set.
  image         Json?
  lastName      String
  name          String
  password      String
  role          String
  timezone      String
  username      String
}

model portal_userNotifications {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}

model portal_userTask {
  id String @id @default(auto()) @map("_id") @db.ObjectId
}
